<h1>Editar Moto</h1>

<div class="card mt-4">
    <div class="card-body">
        <%# Renderiza o partial do formulário reutilizável (_form.html.erb) %>
        <%= render 'form', moto: @motorcycle %>
    </div>
</div>
